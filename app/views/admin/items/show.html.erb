<div class="containa">
    <div class="row">
        <div class="col-xs-1"></div>
        <div class="col-xs-10">
            <h2>商品詳細</h2>
                    <div class="col-xs-4">
                        <%= attachment_image_tag @item, :image, size: "100x100", format: "jpeg", fallback: "rails.png" %>
                    </div>
                    <div class="col-xs-8">
                        <p>商品名 <%= @item.name %></p>
                        <p>説明 <%= @item.introduction %></p>
                        <p>ジャンル <%= @item.genre.name %></p>
                        <p>税抜き価格 <%= @item.price %></p>
                        <p>販売ステータス 
                            <% if @item.is_active %>
                            販売中
                            <% else %>
                            販売停止中
                            <% end %></p>
                        <%= link_to "編集する", edit_admin_item_path, class: "btn btn-info" %>
                    </div>
        </div>
        <div class="col-xs-1"><div>
    </div>
</div>
    