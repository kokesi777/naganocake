
<%= form_for(@genre, url: admin_genres_path) do |f| %>
    <p>ジャンル     <%= f.text_field :name %>
    <label><%= f.radio_button :is_active, "有効" ,checked: "checked" %> 有効 </label>
    <label><%= f.radio_button :is_active, "無効" ,checked: "" %> 無効</label>
    <%= f.submit '追加' %>  </p>
<% end %>