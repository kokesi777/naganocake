<div>
<h3>ジャンル編集</h3>
</div>

<div>
<%= form_for(@genre, url: admin_genre_path(@genre.id)) do |f| %>
    <label>ジャンル</label>
        <%= f.text_field :name %>
        <%= f.radio_button :is_active, "true", checked: "checked" %>
            <%= f.label :is_active, "有効" , value: true %>
        <%= f.radio_button :is_active, "false" %>
            <%= f.label :is_active, "無効" , value: false %>
        <%= f.submit "保存する", class: "btn btn-primary" %>  </p>
<% end %>
</div>
